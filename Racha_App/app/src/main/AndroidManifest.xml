<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.racha">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Racha"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.MaterialComponents.DayNight.DarkActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CadastroActivity"
            android:exported="false"
            android:theme="@style/Theme.Racha" />

        <activity
            android:name=".InicioActivity"
            android:exported="false"
            android:theme="@style/Theme.Racha" />

        <activity
            android:name=".CartoesActivity"
            android:exported="false"
            android:theme="@style/Theme.Racha" />

        <!-- Adicione aqui se houver mais activities, como GruposActivity e NotificacoesActivity -->
        <activity
            android:name=".GruposActivity"
            android:exported="false"
            android:theme="@style/Theme.Racha" />

        <activity
            android:name=".NotificacoesActivity"
            android:exported="false"
            android:theme="@style/Theme.Racha" />

    </application>
</manifest>
